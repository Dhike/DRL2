import PlaceholderPage from "../../components/PlaceholderPage";

export default function ScannerPage() {
  return (
    <PlaceholderPage
      title="Scanner"
      description="Pick a market and strategy, and DRL2 scans the instruments for valid setups."
      phase="Phase 8 (Automatic Scanner)"
    />
  );
}
